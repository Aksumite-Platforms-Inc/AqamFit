class LeaderboardEntry {
  final String name;
  final int points;
  final int rank;
  final String avatar; // Assuming avatar is a path or URL string

  LeaderboardEntry(this.name, this.points, this.rank, this.avatar);
}
